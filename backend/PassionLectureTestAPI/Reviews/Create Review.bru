meta {
  name: Create Review
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/books/{{bookId}}/reviews
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "rating": 5,
    "comment": "Un livre magnifique, à lire absolument !"
  }
}
